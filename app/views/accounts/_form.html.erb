<%= form_for(@account) do |f| %>
  <% if @account.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@account.errors.count, "error") %> prohibited this account from being saved:</h2>

      <ul>
      <% @account.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :username %><br>
    <%= f.text_field :username, :disabled => true  %>
  </div>
  <div class="field">
    <%= f.label :twitter_id %><br>
    <%= f.text_field :twitter_id, :disabled => true  %>
  </div>
  <div class="field">
    <%= f.label :display_name %><br>
    <%= f.text_field :display_name, :disabled => true  %>
  </div>
  <div class="field">
    <%= f.label :party_affiliation_id %><br>
    <%= f.collection_select :party_affiliation_id, PartyAffiliation.all, :id, :partyAffiliation %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
